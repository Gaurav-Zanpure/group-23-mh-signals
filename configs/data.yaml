seed: 42
paths:
  raw_csv: data/raw/final/mh_signal_data_w-concern-intent.csv
  splits_dir: data/splits
  processed_dir: data/processed
  results_dir: results
  llm_tagged_dir: data/llm_tagged
labels:
  concern_map: {low: 0, medium: 1, high: 2}
split:
  test_size: 0.15
  val_size_from_train: 0.1765
text:
  max_len: 256