seed: 42
paths:
  raw_csv: data/raw/mental_health_signal_detector_data.csv
  splits_dir: data/splits
labels:
  concern_map: {low: 0, medium: 1, high: 2}
split:
  test_size: 0.15
  val_size_from_train: 0.1765
text:
  max_len: 256